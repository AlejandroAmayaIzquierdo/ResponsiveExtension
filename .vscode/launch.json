{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${file}",
            "preLaunchTask": "customBuild", // Add this line
            "outFiles": [
                "${workspaceFolder}/out/**/*.js"
            ],
            "runtimeExecutable": "${workspaceFolder}/node_modules/.bin/electron"
        }
    ]
}